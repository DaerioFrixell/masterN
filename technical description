use SCSS 
(create react app sass)

создать app, привязать к git
	on git bucket blocked (Ukraine war)
  --- on gitHub now. It was change later
	on git lab;
	+ настроить git ignore (added this file)

create brunch, but not master/main (later)

+ сверстать форму
+ placeholder на каритнку, пока не загружена.
+ при клике "выбор картинке дэфолтный"
+ при выборе img она отображается
+ routing для форма, хуяции

any validation

	field: login ----no (validation)
	field: email ----no (validation) 
	field: pass ----no (validation)
	field: img (отдельный компонет)
	field: reset for field and for img only
	на кнопку выводить все данные
	(button-handler: переключает тип валидации( при вводе, при сабмите) - low priority)

по дэфолту ничо не светица
при кнопке на сабмит чекалась валидация всех полей
--------

check: BEM library?? low priority



